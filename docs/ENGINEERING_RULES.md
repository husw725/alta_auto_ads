# 🦞 Auto Meta ADS 核心工程准则 (Lobster AI Standard)

> 此文件记录了所有经过验证的黄金规则，后续任何代码修改必须优先查阅此文档，严禁改动已确定的逻辑。

## 1. 命名规范 (Strict Naming Convention)
- **格式**：{剧名}-{国家}-{YYYYMMDD}-w2a-Auto-龙虾ai
- **示例**：FFAS-新-US-20260310-w2a-Auto-龙虾ai
- **注意**：剧名直接取 XMP 文件夹名，保留其版本标识（如 "-新"）。

## 2. 缩略图决策链路 (Thumbnail Strategy)
系统必须按以下优先级获取广告封面图，严禁跳步：
1. **优先 (Native Hash)**：尝试 40 秒 Meta 官方视频抽帧 (8次 * 5秒间隔)。
2. **次选 (XMP Cover)**：若抽帧超时，直接使用 XMP 原始海报 URL。
3. **最终兜底 (Stable S3)**：https://starlitshorts.s3.amazonaws.com/s/986f2dd37aba040d55361a407ca860f5.png

## 3. Meta API 强制参数 (Standard Payload)
- **Objective**: `OUTCOME_APP_PROMOTION` (应用推广)
- **Destination Type**: `APP`
- **Optimization Goal**: `APP_INSTALLS`
- **Special Categories**: `["NONE"]`
- **Bid Strategy**: `LOWEST_COST_WITHOUT_CAP` (自动出价)
- **Budget Type**: CBO (预算必须设在 Campaign 层级，单位：分)
- **Targeting**: 必须锁定 `user_os: ["iOS"]` (以匹配 iTunes 链接)
- **CTA**: `INSTALL_APP`

## 4. 数据看板字段 (15 Metrics)
看板和日报必须包含且不限于：
- 广告ID, 广告名称, 创建时间, 投放日期
- 花费(Spend), 预算(Budget), 点击(Click), 点击率(CTR)
- 安装(Install), ROI, 转化率(CVR), CPM, CPC, CPI, CPP

## 5. 自动调优逻辑 (Auto-Optimization)
- **执行时间**：每天日报发送前。
- **动作**：检测到 CPI > 阈值且消耗 > 最小判定值时，**强制暂停**该广告。
- **汇报**：在日报中明确列出“自动调优战报”。

## 6. UI/UX 规范
- **默认页面**：AI 投流助手 (Sidebar Index 0)。
- **交互**：看板必须包含“激活/暂停”一键开关。
- **记忆机制**：AI 助手必须具备上下文关联能力（支持回复数字选择剧集）。

## 7. 缓存策略
- **XMP 目录**：persist="disk", TTL=18h。
- **XMP 素材**：persist="disk", TTL=8h。

---
*Generated by Gemini CLI Agent · 龙虾AI 投流系统核心宪法*
