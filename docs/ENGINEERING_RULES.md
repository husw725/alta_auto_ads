# 🦞 Auto Meta ADS 核心工程准则 (Lobster AI Standard)

## 0. 核心红线：零功能回退 (NO REGRESSION)
- **严禁改动无关功能**：在迭代新功能或修复局部 Bug 时，必须确保现有 UI 元素（特别是 KPI 汇总、数据看板、控制按钮）不被误删或移动。
- **验证清单**：每次修改 `app.py` 后，必须自检：
    1. 顶部 4 大 KPI 卡片是否显示？
    2. 15 个全量指标表格是否完整？
    3. 激活/暂停/删除三键是否存续？
    4. 日期切换逻辑是否正常？

## 1. 命名规范 (Strict Naming Convention)
- **格式**：{剧名}-{国家}-{YYYYMMDD}-w2a-Auto-龙虾ai

## 2. 缩略图决策链路 (Thumbnail Strategy)
1. **优先 (XMP Cover)**：直接使用 XMP 原始海报 URL (极速模式)。
2. **次选 (Native Hash)**：若无 XMP 图，尝试 Meta 官方视频抽帧。

## 3. 智能调优规则 (Optimization Rules 2.0)
| 规则名称 | 触发条件 | 执行动作 |
| :--- | :--- | :--- |
| **暂停劣质** | CPI > 阈值 且 花费 > $50 | 暂停广告 |
| **降低预算** | CPI > 阈值*0.8 且 花费 > $30 | 降低日预算 50% |
| **提升预算** | CPI < 阈值*0.6 且 ROI > 目标*1.2 | 提升日预算 30% |
| **批量优化** | 支持一键执行所有“低风险”安全优化动作 | 批量执行 |

## 4. 数据看板与日期规范
- **时区锁定**：必须强制对齐 **UTC-8** 太平洋时间。
- **日期透明**：看板必须提供“昨天/今天”切换，并明确标注日期字符串。

---
*Generated by Gemini CLI Agent · 龙虾AI 零回退稳定版宪法*
