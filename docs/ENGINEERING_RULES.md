# 🦞 Auto Meta ADS 核心工程准则 (Lobster AI Standard)

## 🚨 第 0 号最高准则：零功能回退 (ZERO REGRESSION)
- **绝对不简化**：严禁在实现新功能时，移除或简化已存在的逻辑（如语义匹配、40秒探测降级、多重保底图片等）。
- **完整性锁定**：在修改 `app.py` 时，必须确保以下组件在任何版本下都不可消失：
    1.  **AI 语义匹配**：基于 GPT-4o 的模糊剧名搜索。
    2.  **顶部 KPI 指标**：总消耗、安装、ROI、CPI 的四项汇总卡片。
    3.  **15 维表格**：包含 Spend, imps, Click, CTR, Install, ROI, CVR, CPM, CPC, CPI, CPP。
    4.  **生命周期按钮**：👁️预览、🟢激活、🟡暂停、🗑️删除。
    5.  **时区对齐**：强制锁定 UTC-8 (PST) 及日期切换标签。

## 1. 命名规范 (Strict Naming Convention)
- **格式**：{剧名}-{国家}-{YYYYMMDD}-w2a-Auto-龙虾ai

## 2. 缩略图决策链路 (Thumbnail Strategy)
1. **优先 (XMP Cover)**：直接使用 XMP 原始海报 URL (极速模式)。
2. **次选 (Native Hash)**：若无 XMP 图，尝试 Meta 官方视频抽帧。

## 3. 智能调优规则 2.0 (Optimization)
- **暂停劣质**：CPI > 阈值 且 Spend > $50。
- **批量优化**：支持一键执行所有低风险项。
- **高风险熔断**：Spend > $200 的操作必须手动确认。

## 4. 环境独立性 (Portability)
- **零依赖发送**：钉钉发送代码内置化，不引用外部路径。
- **venv 继承**：使用 `sys.executable` 确保环境一致。

---
*Generated by Gemini CLI Agent · 龙虾AI 最高行为准则*
